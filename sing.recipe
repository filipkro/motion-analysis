Bootstrap: docker

From: continuumio/miniconda3

%files
    motion.yml

%post
    apt update
    apt install g++ -y
    /opt/conda/bin/conda env create -f motion.yml

%runscript
    exec /opt/conda/bin/conda/envs/$(head -n 1 environment.yml | cut -f 2 -d ' ')/bin/"$@"
